export const sendNotification = async (userId, notificationData) => {
    // Example logic for sending an email or in-app notification
    // Replace with your notification system (e.g., email, SMS, in-app)
    console.log(`Sending notification to user ${userId}: ${notificationData.message}`);

    // Example: You could integrate with an email service like SendGrid, Nodemailer, etc.
    // For now, this is just a mock implementation that logs the notification.
};
